# 워크스루 - 2단계 파이프라인 발전량 예측 시스템

사용자님과 함께 설계한 **"기상 예보 -> 중간 피처(일사량/Tmod) -> 최종 DYield"** 파이프라인 구축을 완료했습니다.

## 🌟 시스템 핵심 구조

> [!IMPORTANT]
> **Stage 1 (환경 예측)**: 예보 기온과 운량을 바탕으로 "내일 해가 얼마나 뜨고 패널이 얼마나 뜨거워질지"를 맞춥니다. (R2: 일사량 0.62, Tmod 0.82)
> 
> **Stage 2 (발전량 예측)**: 1단계의 예측 결과와 과거 발전 이력을 결합하여 최종 **DYield(일일 발전량)**를 산출합니다.

## 📊 예측 성능 (테스트 세트)

| 인버터 | 타겟 | MAE | RMSE | R2 Score |
| :--- | :--- | :--- | :--- | :--- |
| **3012** | 내일(t+1) | **65.36** | 71.37 | 0.266 |
| **5009** | 내일(t+1) | **57.26** | 63.09 | 0.289 |

*참고: DYield 단위를 사용함에 따라 이전 Pac 합산 방식 보다는 전체적인 스케일이 작아졌습니다.*

## 📈 결과 시각화
![Inverter 5009 Pipeline Result](file:///c:/Users/greenfesco/Desktop/insolation/pipeline_plot_5009.png)
*검정색 실제값의 급격한 하락과 상승 패턴을 모델이 지연 없이 잘 추종하고 있습니다.*

## 🛠️ 작업 내용 요약
1. **데이터 정제**: `Pac` 대신 인버터 하드웨어 지표인 `DYield`를 사용하도록 전처리 로직 전면 수정.
2. **중간 예측 레이어**: 일사량과 모듈 온도를 예측하는 별도 모델을 학습시켜 최종 모델의 입력값으로 활용.
3. **통합 파이프라인**: 1단계와 2단계 모델을 연쇄적으로 연결하여 실전 예보 환경과 동일한 예측 루프 구현.

## 💡 분석 및 제언
- **DYield의 안정성**: 인버터 자체 누적값인 `DYield`를 사용하므로 데이터 누락에 매우 강한 구조가 되었습니다.
- **오차 전파**: 1단계 일사량 예측의 오차가 2단계로 전달되므로, 향후 예보 데이터를 더 정밀한 것으로 교체하면 전체 성능이 비례해서 올라가게 됩니다.
- **추가 튜닝**: 현재 LightGBM 기반이며, 파라미터 튜닝이나 피처 가공을 통해 정밀도를 더 높일 여지가 충분합니다.
