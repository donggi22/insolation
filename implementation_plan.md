# 태양광 인버터 모델을 위한 데이터 전처리 계획

이 계획은 인버터, 센서, 날씨라는 세 가지 서로 다른 데이터 소스를 모델링에 적합한 5분 단위의 통합 형식으로 정리하고 병합하는 단계를 설명합니다. 요청하신 대로 인버터 3012와 5009를 위한 두 개의 개별 데이터셋을 생성할 예정입니다.

## 사용자 검토 필요 사항

> [!IMPORTANT]
> **스트링 데이터 유지**: `Upv1-3` 및 `Ipv1-3`을 합산하거나 평균내지 않고, 각각 개별 피처로 유지하겠습니다.
> **PF(역률) 처리**: 현재 1.0이 100%를 의미하므로 별도의 스케일링(100으로 나누기)을 수행하지 않고 원본 값을 유지하겠습니다.
> **실행 환경**: 일반 터미널에서 `pandas`를 사용할 수 없는 경우, Jupyter Notebook과 동일한 환경에서 스크립트를 실행해 주시기 바랍니다.

## 제안된 변경 사항

### 1. 인버터 데이터 전처리 (`total_power.csv`)
- **필터링**: SN 끝자리가 `3012` 및 `5009`인 데이터만 유지.
- **PF 처리**: 별도의 나눗셈 없이 원본 데이터 유지 (1.0 = 100%).
- **피처 유지**: `Upv1`, `Upv2`, `Upv3`, `Ipv1`, `Ipv2`, `Ipv3`를 개별 컬럼으로 모두 유지.
- **불필요 컬럼 제거**: 요청하신 '쓰레기 값' 및 메타데이터 컬럼 삭제 (`LCDBoot_ARCVersion`, `ARCStatus_PVinputConfig`, `ProVer`, `MinorVer`, `RWRegSum`, `RWRegAdd`, `Tcoil`, `Time`, `use_yn`, `crt_usr`, `udt_dttm`, `Pfault`, `Warm`, `Fault0-4`, `DSPSecuritySpecificationVersionNumr` 등).
- **시간 정렬**: `crt_dttm`을 datetime 형식으로 변환하고 5분 단위로 내림(floor) 처리.

### 2. 센서 데이터 전처리 (`total_IoT_sensor.csv`)
- **피처 선택**: `light_intensity`, `temperature`, `humidity`, `wind_speed`, `uv_index`.
- **배터리 처리**: 문자열로 변환하거나 초기 모델에서는 제거 (제거 권장).
- **시간 정렬**: `create_at`을 datetime으로 변환하고 5분 단위로 그룹화하여 평균값(`mean()`) 산출.

### 3. 날씨 데이터 전처리 (`moa_weather.csv`)
- **타임스탬프 생성**: `base_date`와 `base_time`을 결합하여 생성.
- **피처 선택**: `sky`, `t1h`, `reh`, `wsd`.
- **리샘플링**: 1시간 단위 데이터를 선형 보간(`interpolate`) 또는 이전 값 채우기(`ffill`)를 통해 5분 단위로 확장.

### 4. 병합 및 출력
- **조인(Join)**: 인버터 데이터를 기준으로 센서와 날씨 데이터를 타임스탬프에 맞춰 왼쪽 조인(`left join`).
- **결과물**: 다음 두 개의 파일로 저장:
  - `preprocessed_3012.csv`
  - `preprocessed_5009.csv`

## 해결이 필요한 질문

1. **날씨 보간 방식**: 1시간에서 5분으로 확장할 때 `ffill`(이전 값 복사)과 `interpolate`(선형 보간) 중 어떤 방식을 선호하시나요? (온도/습도 등은 보간법이 더 자연스럽습니다.)

## 검증 계획

### 자동 검증
- 병합 후 결측치(`null`) 발생 여부 확인.
- 최종 데이터 구조의 컬럼 수와 데이터 타입이 요구사항과 일치하는지 확인.
- PF 값이 0~1 범위 내에 있는지 확인.

### 수동 검증
- 생성된 CSV 파일의 앞부분과 뒷부분 데이터 확인.
- 시간대별 데이터 정렬 상태 및 경계값 확인.
