# 데이터 고도화 계획: 기상 데이터(Weather) 통합

1시간 단위의 기상청 데이터(`moa_weather`)를 5분 단위로 보간하여 통합함으로써, 모델이 거시적인 기상 상황(흐림, 비 등)을 인지하게 하여 '지름길' 문제를 완화합니다.

## 주요 작업 내역

### 1. 전처리 파이프라인 업데이트 (`preprocess_data.py`)
- **`moa_weather.csv` 통합**:
    - **시간축 변환**: `tm` 컬럼을 datetime 형식으로 변환.
    - **5분 단위 리샘플링**: 1시간 데이터를 5분 단위 빈 슬롯으로 확장.
    - **지능형 보간(Interpolation)**:
        - 기온(`tmp`), 습도(`reh`), 풍속(`ws`), 강수확률(`pop`) 등 연속형 변수는 **선형 보간(Linear)** 적용.
        - 하늘 상태(`sky`), 강수 형태(`pty`), 풍향(`wd`) 등 범주형 변수는 **직전 값 유지(Forward Fill)** 적용.
    - **최종 병합**: 기존 [인버터 + 센서] 데이터와 `tm` 기준 Left Join.

### 2. 모델 학습 전략 업데이트 (`train_model.py`)
- **피처 확장**: 보간된 기상 피처들(`sky`, `pty`, `pop`, `tmp_weather` 등)을 모델 입력값에 추가.
- **Fair Comparison 재실행**: 기상 데이터가 추가된 상태에서 [Baseline, Memory, Edge] 모델을 다시 평가하여 성능 향상 폭 측정.

### 3. 결과 분석 및 보고
- 기상 데이터 추가가 특히 **'급변 구간(Spikes)'**의 예측에 어떤 영향을 주었는지 집중 분석.
- 최종 통합 데이터셋 구조 보고.

## 기대 효과
- 모델이 "지금은 흐린 날씨의 연장선"임을 인지하게 되어, 센서의 일시적인 노이즈에 휘둘리지 않고 더 안정적이고 정확한 피크 예측이 가능해집니다.

## 해결이 필요한 질문

1. **컬럼명 중복 처리**: 센서 데이터의 온도/습도(`temperature`, `humidity`)와 기상 데이터의 온도/습도(`tmp`, `reh`)가 모두 존재합니다. 둘 다 사용할까요? (센서는 실제 현장 데이터, 기상은 예보/관측 데이터로 두 데이터의 차이 자체가 유용한 정보가 될 수 있습니다.)

## 검증 계획
- 전처리 후 기상 데이터 필드가 5분 단위로 끊김 없이(NaN 없이) 잘 채워졌는지 확인.
- 기상 데이터 추가 전후의 R2 Score 및 MAE 개선율 비교.
